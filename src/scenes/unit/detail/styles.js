const styles = theme => ({
  formField: {
    padding: theme.spacing(1)
  },
  formButton: {
    // padding: theme.spacing(2),
    float: "right"
  },
  backButton: {
    // padding: theme.spacing(2),
    justifyContent: "default",
    background: "#64dd17",
    color: "white"
  }
});

export default styles;
